# =============================================================================
# API Territoires - Variables d'environnement
# =============================================================================
# Copier ce fichier en .env et adapter les valeurs
# cp .env.example .env

# --- Base de données PostgreSQL + PostGIS ---
# En Docker (via docker-compose) :
DATABASE_URL="postgresql://territoires:territoires2026@territoires-db:5432/territoires?schema=public"
# En local (si PostgreSQL installé sur la machine) :
# DATABASE_URL="postgresql://territoires:territoires2026@localhost:5432/territoires?schema=public"

# --- JWT Secret (authentification admin) ---
# OBLIGATOIRE en production. Générer avec : openssl rand -base64 48
# En développement, le fallback "dev-only-secret" est utilisé automatiquement.
JWT_SECRET=""

# --- Redis (cache, optionnel) ---
# Si absent, l'API fonctionne sans cache (dégradation gracieuse)
# REDIS_URL="redis://localhost:6379"

# --- Connexions externes (scripts d'import uniquement) ---
# Nécessaire uniquement pour les scripts qui importent depuis d'autres bases
# CARTE_DATABASE_URL="postgresql://user:password@host:5432/carte?schema=public"
# CARTO_DATABASE_URL="postgresql://user:password@host:5432/carto_payload"

# --- Environnement ---
NODE_ENV="development"
